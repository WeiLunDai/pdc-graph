
# source for graphDlib
src_source = ['gui.cc', 'graph.cc']

# generate shared library
graphDlib = shared_library('graphDisplay',
    src_source, 
    include_directories : inc, 
    dependencies : lib_dep,
    install : true)

